{
  "name": "cardano-websocket-server",
  "version": "1.0.0",
  "description": "WebSocket server for real-time comments",
  "main": "server.js",
  "scripts": {
    "build": "echo 'WebSocket server build completed'",
    "start": "node ../scripts/websocket-server.js",
    "dev": "nodemon ../scripts/websocket-server.js"
  },
  "dependencies": {
    "ws": "^8.14.2",
    "@prisma/client": "^5.7.1"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
